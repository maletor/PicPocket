<%= image_tag @photo.photo.url %>
<%= link_to "#{@photo.created_at}", @photo.photo.url, :popup => ["height=300,width=600"] %>
<%= @photo.flag_count %>
<%= link_to_unless @photo.flagged?, 'Flag this', flag_photo_path(@photo), :method => :put %>